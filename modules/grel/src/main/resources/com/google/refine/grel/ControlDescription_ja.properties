filter_desc=配列の各要素を変数名に代入し、式を評価してtrueを返すものだけを配列として返す.
foreach_desc=配列の各要素を変数名に代入し、式を評価してtrueを返すものだけを配列として返す.
foreach_index_desc=配列の各要素を変数名に、インデックスを変数iに代入し、式を評価した結果を配列として返す.
for_non_blank_desc=空白でない各要素を変数名に代入し、空白でない場合は式eNonBlankを、空白の場合は式eBlankを評価した結果を配列として返す.
for_range_desc=fromからto未満までの増分stepで増加する変数vで、式を評価した結果を配列として返す.
if_desc=式oを評価し、trueなら式eTrueを、falseなら式eFalseを評価した結果を返す.
is_blank_desc=oがnullか空文字かどうかを真偽値で返す
isempty_string_desc=oが空文字かどうかを真偽値で返す
is_error_desc=oがエラーかどうかを真偽値で返す
is_non_blank_desc=oがnullでなく空文字でもないかどうかを真偽値で返す
is_not_null_desc=oがnullではないかどうかを真偽値で返す
is_null_desc=oがnullかどうかを真偽値で返す
is_numeric_desc=oが数値としてみなせるかどうかを真偽値で返す
is_test_desc=oが数値としてみなせるかどうかを真偽値で返す
with_desc=式oを評価し、その値を変数vに代入して式eを評価した結果を返す
