describe(__filename, function () {
	it('Import a schema from a local file', function () {});

	it('Import a schema by copy-pasting', function () {});

	it('Import an invalid schema by copy-pasting', function () {});

	it('Close the panel', function () {});
});
