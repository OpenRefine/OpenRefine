describe(__filename, function () {
	it('Login with invalid credentials', function () {});

	it('Close the panel', function () {});

	it('Owner only consumer credentials panel', function () {});

	it('Owner only consumer credentials panel, switch back to regular login', function () {});
});
